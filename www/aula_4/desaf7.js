// Crie um array de números inteiros. Some todos os elementos e mostre o resultado no console.let numeros = [5, 8, 3, 10];

let numeros = [5, 8, 3, 10];

let soma = 0;
for (let i = 0; i < numeros.length; i++) {
  soma += numeros[i];
}

console.log("A soma dos números é:", soma);
